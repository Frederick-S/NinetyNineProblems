version: 2
jobs:
  build:
    working_directory: /temp
    docker:
      - image: microsoft/dotnet:sdk
    steps:
      - checkout
      - run: apt-get install make
      - run: make build
      - run: make test